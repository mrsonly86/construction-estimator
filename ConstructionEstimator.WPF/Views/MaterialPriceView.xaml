<Window x:Class="ConstructionEstimator.WPF.Views.MaterialPriceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bảng Giá Vật Liệu" Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="BẢNG GIÁ VẬT LIỆU XÂY DỰNG" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="10"/>
        
        <!-- Filter Section -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Label Content="Chọn tỉnh/thành:" VerticalAlignment="Center"/>
            <ComboBox Width="200" Margin="5,0" 
                      ItemsSource="{Binding SupportedProvinces}"
                      SelectedItem="{Binding SelectedProvince}"
                      DisplayMemberPath="."
                      SelectedValuePath="."/>
            <Button Content="Làm mới" Margin="10,0" Padding="10,5"
                    Command="{Binding RefreshCommand}"/>
        </StackPanel>
        
        <!-- Data Grid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding MaterialPrices}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tên vật liệu" Binding="{Binding MaterialName}" Width="150"/>
                <DataGridTextColumn Header="Tỉnh/Thành" Binding="{Binding Province}" Width="120"/>
                <DataGridTextColumn Header="Giá (VND)" Binding="{Binding Price, StringFormat='{}{0:N0}'}" Width="120"/>
                <DataGridTextColumn Header="Đơn vị" Binding="{Binding Unit}" Width="80"/>
                <DataGridTextColumn Header="Ngày hiệu lực" Binding="{Binding EffectiveDate, StringFormat='dd/MM/yyyy'}" Width="100"/>
                <DataGridTextColumn Header="Cập nhật lúc" Binding="{Binding LastUpdated, StringFormat='dd/MM/yyyy HH:mm'}" Width="130"/>
                <DataGridTextColumn Header="Nguồn" Binding="{Binding Source}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding MaterialPrices.Count, StringFormat='Tổng số: {0} vật liệu'}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding IsLoading, Converter={StaticResource BoolToLoadingTextConverter}}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>